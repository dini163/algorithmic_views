/* 面试算法 59 题文案增强：问题描述（q）+ 优化前后复杂度对比（cp）
   键名 mN（N=1..59），与 data_m.js 的 no 一致，加 m 前缀避免与经典谜题撞键 */
(function () {
  PZ.desc = PZ.desc || {};
  var X = PZ.desc;

  /* ---- 第一辑 游戏与策略（m1..m17） ---- */
  X.m1 = { q: '写一个程序，让 Windows 任务管理器里的 CPU 占用率曲线听你指挥：想让它保持在某个固定值（如 50%），甚至画出一条正弦波。怎么做？', cp: '朴素：写死循环空转 → 占用率恒 100%、无法调节 → 优化：忙/睡占空比——每个 40ms 周期里 busy 一段时间再 Sleep，占用率 = busy/(busy+sleep)，按正弦表动态调 busy 即可画出任意波形，O(1) 控制。' };
  X.m2 = { q: '中国象棋的将、帅只能在各自九宫（3×3）内移动，且不能在同一列"面对面"。只允许用一个变量，枚举出所有合法的将帅位置组合，共多少种？', cp: '朴素：双重循环各 9 → 需两个循环变量，违反"只用一个变量" → 优化：位运算——一个字节低 4 位存帅、高 4 位存将，i 从 0 数到 80 枚举全部 81 组合，过滤同列 9 种，剩 54 种，O(81) 单变量。' };
  X.m3 = { q: '一种书 25 元，一起买的不同卷数越多折扣越大（2 卷 5%、3 卷 10%、4 卷 20%、5 卷 25%，同种多本拆到不同组不叠加折扣）。买一批书（可能同种多本）如何分组使总价最低？', cp: '朴素贪心：每轮凑满 5 种 → 在 8 本（5+3）时翻车，161.25 元 > 4+4 的 160 元 → 优化：DP——状态为"各卷剩余本数"，每步枚举组大小 k 取最小，全局最优；贪心 O(n) 但可能错，DP 状态×5 转移保证正确。' };
  X.m4 = { q: 'N 台机器中有 1 台故障，它生产的产品每件重 9 克（正常每件 10 克）。用一台能显示读数的秤，最少称几次一定能找出故障机器？', cp: '朴素：逐台称重排查 → 最坏 N−1 次 → 优化：编码法——从第 k 台取 k 件一起称，全正常应重 (Σk)×10 克，实际少 d 克即第 d 台故障，1 次 O(1) 定位。' };
  X.m5 = { q: '货舱体积上限 V，第 i 种饮料有体积 vi、幸福度 Hi、最多供货 Bi 瓶，如何装载使总幸福度最大？', cp: '朴素：枚举每种拿几瓶，指数级 → 优化：背包 DP——f(x)=体积 x 的最大幸福度，完全背包正序、01 背包倒序、多重背包二进制拆分，O(M×V×logB) 伪多项式；密度贪心不保证最优。' };
  X.m6 = { q: 'n 条光线（直线）两两相交且无三线共点，最多把平面分成多少块？n 条线段最多有多少个交点？', cp: '朴素：逐条画线数块 → O(n²) 且要处理浮点精度 → 优化：递推闭式——第 k 条线被前 k−1 条切成 k 段、块数 +k，累加得 R(n)=1+n(n+1)/2；交点数 C(n,2)=n(n−1)/2，O(1) 直接算。' };
  X.m7 = { q: '电梯从 1 层出发载所有人上行，只在某一层 k 停一次，之后大家爬楼梯到各自目标层。停在哪一层能让所有人爬的楼层总数最小？', cp: '朴素：对每个 k 逐人累加 |目标层−k| → O(N²) → 优化：代价 C(k) 是凸函数，改停 k+1 时下面人多爬 1 层、上面人少爬 1 层，当"上面人数≥下面人数"即停 → 最优层是人数中位层，前缀和后 O(N) 扫描。' };
  X.m8 = { q: 'N 位应聘者各报若干可参加的时间段，要求每人恰好安排一个时间段且任意两人不撞在同一时段。如何高效判断能否安排并给出方案？', cp: '朴素：顺序贪心分配、遇占用即卡死 → 可能漏掉可行解 → 优化：二部图最大匹配（匈牙利算法）——逐人找增广路、占用者可"改嫁"，O(V×E) 得到最大匹配，配满即成功。' };
  X.m9 = { q: 'n 个文件、两个下载线程，文件不可切开，如何分配使两个线程尽量同时收工（较忙线程耗时最小）？', cp: '朴素：贪心把大文件给空闲线程 → 不保证最优 → 优化：子集和 DP——f(s)=A 线程能否恰好背出耗时和 s，在 s≤总和/2 里找最大可达 s，O(n×sum/2)；两组差最小即最均衡。' };
  X.m10 = { q: '一排 N 个石头，两人轮流取，每次取 1..M 个，取走最后一个的赢。先手有必胜策略吗？第一步该取几个？', cp: '朴素：模拟对局/搜索博弈树 → 指数级 → 优化：必败态 = (M+1) 的倍数，N mod (M+1)≠0 先手胜、首步取余数，O(1) 判定（巴什博弈）。' };
  X.m11 = { q: '多堆石头，每次从某一堆取任意多个，取走最后一个的赢。如何判断当前局面先手必胜还是必败？', cp: '朴素：博弈树搜索 → 指数级 → 优化：Sprague-Grundy——各堆数量异或和 X，X≠0 必胜、X=0 必败，O(堆数) 判定并 O(堆数) 找平衡步。' };
  X.m12 = { q: '两堆石头，每次从一堆取任意个、或两堆同取相同个数，取光者赢。哪些 (a,b) 是必败态？如何 O(1) 判定？', cp: '朴素：博弈树/打表 → 指数或 O(范围²) → 优化：威佐夫博弈——必败态 (⌊kφ⌋, kφ⌋+k)，φ=(1+√5)/2，给定 (a,b) 取 k=b−a 比较 a 与 ⌊kφ，O(1)。' };
  X.m13 = { q: '连连看棋盘中两张相同的牌，若能用转弯不超过 2 次、只经过空格的折线连通即可消除。如何高效判定一对牌能否消除？', cp: '朴素：盲目 DFS 所有路径 → 指数级 → 优化：按转弯数分层 BFS（或枚举 0/1/2 折三种连线），每格最多 3 个转弯状态，O(格数×方向×转弯)。' };
  X.m14 = { q: '不要求解数独，而是快速"造"出一个行、列、九宫均不重复的合法数独盘，并能派生出大量不同盘面。怎么做？', cp: '朴素：回溯搜索填盘 → 可能很慢 → 优化：闭式模板 grid[r][c]=(3(r%3)+⌊r/3⌋+c)%9+1 一次 O(81) 填出合法盘，再叠加保持合法的行/列/带交换与数字重命名派生海量盘面。' };
  X.m15 = { q: '给 4 个数，每个数恰好用一次，用 +−×÷ 和括号凑出 24。如何系统判定有无解（如 [3,3,8,8]）？', cp: '朴素：只试整数中间值/漏括号结构 → 漏解 → 优化：两两合并 DFS 枚举全部运算与括号结构（4!×4³×5=7680 种），用分数/容差处理中间值，O(常数) 可解。' };
  X.m16 = { q: '俄罗斯方块的核心循环（碰撞检测、落定、消行、旋转）如何用位运算做到极快？', cp: '朴素：逐格二维数组判断碰撞/消行 → O(宽×高) 且分支多 → 优化：行位掩码——碰撞=AND、落定=OR、满行=等值比较、旋转查表，核心操作 O(1)/O(高) 位运算。' };
  X.m17 = { q: '扫雷中每个数字等于周围 8 格的地雷数。如何生成数字盘？又有哪些基本推理规则可以确定雷位与安全格？', cp: '生成：每格数 8 邻居 O(格数×8)。推理：规则①数字==周围未开格数→全雷；规则②数字已被标雷满足→其余安全；两条规则交替可解大部分局面，O(格数)。' };

  /* ---- 第二辑 数字与数组（m18..m38） ---- */
  X.m18 = { q: '对于一个字节变量 v，如何求它的二进制表示中 1 的个数？要求尽量少地执行位运算。', cp: '朴素：逐位右移检查 32 次 → O(位数) → 优化：v &= (v−1) 每次恰好消掉最低位的 1，循环次数 = 1 的个数；或查 256 项表 O(1)，比逐位法快得多。' };
  X.m19 = { q: '① 100! 的末尾有多少个 0？② N! 的二进制中最低位的 1 在第几位？', cp: '朴素：算出阶乘再数 → 大数溢出、O(N) 乘法 → 优化：① 0 的个数 = 因子 5 的个数 = ⌊N/5⌋+⌊N/25⌋+… = 24（N=100）；② 最低位 1 的位置 = N − popcount(N)，纯数论 O(logN)。' };
  X.m20 = { q: '论坛里发帖超过一半的"水王"删掉了自己的 ID 记录，只剩发帖 ID 列表。如何在 O(N) 时间、O(1) 空间找出水王的 ID？', cp: '朴素：哈希计数 O(N) 空间 → 排序取中位数 O(NlogN) → 优化：Boyer-Moore 多数投票——相同则计数 +1、不同则 −1、归零换人，幸存者再扫一遍验证，O(N) 时间 O(1) 空间。' };
  X.m21 = { q: '给定正整数 N，求 1 到 N 之间所有整数中 1 出现的总次数 f(N)。例如 N=12 时 f(N)=5。', cp: '朴素：逐个数字拆分统计 O(N×logN) → 优化：按位贡献——对每一位拆成高位/当前位/低位三段分类计数，O(logN) 一次算完；再证明 f(N)=N 的解按位构造。' };
  X.m22 = { q: '海量整数中找最大的 K 个（K 相对总量很小），内存放不下全部数据，怎么办？', cp: '朴素：全排序 O(NlogN) 内存爆 → 优化：大小为 K 的最小堆——比堆顶大才替换，O(NlogK) 且只占 K 个元素；或快速选择平均 O(N)；数据分布已知时可多机分治归并。' };
  X.m23 = { q: '二进制浮点数无法精确表示大多数十进制小数（如 0.1）。如何用分数精确逼近一个浮点数？', cp: '朴素：定点放大 10^k → 分母爆炸且仍不精确 → 优化：连分数展开——反复取整数部、倒置小数部，逐层得到收敛子 3/1、22/7、333/106、355/113，误差随层数指数下降，小分母即得高精度逼近。' };
  X.m24 = { q: '求两个正整数的最大公约数。欧几里得算法要取模，若 CPU 取模很慢怎么办？', cp: '朴素：辗转相除 O(log max)，取模开销大 → 优化：更相减损术只用减法但可能减很多轮 → Stein 算法：偶数提公因子 2、奇数用减法，全程只有移位与减法，O(log max) 且无乘除。' };
  X.m25 = { q: '求满足 N! 末尾 0 的个数 ≥ Q 的最小正整数 N（Q 给定）。', cp: '朴素：从 1 递增逐个算 zeros(N!) → O(N logN) 次除法 → 优化：zeros(N) 单调不减 → 对 N 二分答案，每次验证 O(logN)，总 O(log²Q) 量级；Q=4 → N=25。' };
  X.m26 = { q: '求斐波那契数列第 n 项。递归重复计算、迭代 O(n)，能否更快？', cp: '朴素：递归 O(2^n)；迭代 O(n) → 优化：矩阵快速幂 [[1,1],[1,0]]^n 二分幂乘，O(logn) 次 2×2 矩阵乘法；也可用通项公式但有浮点误差。' };
  X.m27 = { q: '在 n 个数中同时找出最大值和最小值，最少需要多少次比较？', cp: '朴素：分别扫两趟 2(n−1) 次；或一趟各维护 max/min 仍需约 2n 次 → 优化：两两配对——每对先内部比 1 次，胜者进 max 组、败者进 min 组各再比 1 次，共 ⌈3n/2⌉−2 次，理论下界。' };
  X.m28 = { q: '平面上 n 个点，求距离最近的一对点。暴力两两比较是 O(n²)。', cp: '朴素：两两枚举 O(n²) → 优化：分治——按 x 中位数分半各自递归，合并时只检查中线 ±δ 条带内的点（按 y 排序后每点最多比 7 个），T(n)=2T(n/2)+O(n) → O(nlogn)。' };
  X.m29 = { q: '给一个整数数组和一个目标和 sum，快速判断是否存在两个数之和等于 sum。', cp: '朴素：双重循环 O(n²) → 优化：① 哈希集合边扫边查 sum−a，O(n) 时间 O(n) 空间；② 数组有序则双指针两端夹逼，O(n) 时间 O(1) 空间。' };
  X.m30 = { q: '数组中有正有负，求连续子数组的最大乘积。', cp: '朴素：枚举所有子数组 O(n²) → 优化：负数会把最大变最小——同时维护 maxEnd/minEnd 两个状态，遇负数交换二者再转移，O(n) 一次扫描。' };
  X.m31 = { q: '求数组中连续子数组之和的最大值（允许全负时取最大单元素）。', cp: '朴素：枚举所有子数组 O(n²) → 优化：Kadane——cur = max(a, cur+a)（前缀为负就丢弃重开），O(n) 一次扫描；[1,−2,3,5,−3,2] → 8。' };
  X.m32 = { q: '求二维矩阵中子矩阵之和的最大值（最大子段和的二维版）。', cp: '朴素：枚举所有子矩阵 O(n²m²) → 优化：压缩维度——固定上下边界 r1..r2 把列压成一维数组跑 Kadane，O(n²m)；配合前缀和 O(1) 求区间列和。' };
  X.m33 = { q: '求数组中最长递增子序列（LIS，可不连续）的长度。', cp: '朴素：DP f(i)=max{f(j)}+1，O(n²) → 优化：tails 数组 + 二分——tails[k] 存长度 k+1 的递增子序列的最小结尾，每元素二分插入，O(nlogn)。' };
  X.m34 = { q: '把数组循环右移 k 位（如 ab → ba），只允许 O(n) 时间、O(1) 额外空间。', cp: '朴素：借助辅助数组 O(n) 空间；逐位挪 k 轮 O(nk) → 优化：三次翻转——先翻前 n−k 个、再翻后 k 个、最后整体翻转，O(n) 时间 O(1) 空间原地完成。' };
  X.m35 = { q: '把 2n 个正整数分成两组各 n 个，使两组和之差最小（数组分割）。', cp: '朴素：枚举 C(2n,n) 种分法 → 指数级 → 优化：DP f(c,s) = 能否恰好选 c 个数凑出和 s，目标是最接近总和一半的 s，O(n²×sum) 伪多项式。' };
  X.m36 = { q: '给若干区间，如何快速判断它们是否有重合？重合判断在排课、订会议室等场景常见。', cp: '朴素：两两比较 O(n²) → 优化：按左端点排序后一次扫描——若当前区间左端 < 前一个的右端即重合（或合并），O(nlogn) 排序 + O(n) 扫描。' };
  X.m37 = { q: '一段嵌套循环代码（外层 i 从 1..n、内层 j 从 i..n 各执行一次基本操作），不用运行程序，直接算出总执行次数并分析复杂度。', cp: '朴素：跑起来计数 O(n²) → 优化：数学求和——Σ(i=1..n)(n−i+1) = n(n+1)/2，闭式 O(1) 得出，复杂度 Θ(n²)，系数 1/2。' };
  X.m38 = { q: '哪些正整数能写成两个及以上连续正整数之和？如 9=4+5=2+3+4。给 N 如何判定？', cp: '朴素：枚举起点与长度 O(N√N) → 优化：N 能表示为连续和 ⟺ N 不是 2 的幂——因为和的表达式含奇因子 (2a+k−1)k/2，2 的幂无奇因子，O(logN) 判定。' };

  /* ---- 第三辑 数据结构（m39..m49） ---- */
  X.m39 = { q: '判断字符串 s2 能否由 s1 循环移位得到（如 AABCD 移位出 CDAA）。要求只调用一次字符串查找函数。', cp: '朴素：逐个移位再比较 O(n²) → 优化：拼接 s1s1，则一切移位都是其子串——长度相等 + contains(s1+s1, s2)，配 KMP 共 O(n)。' };
  X.m40 = { q: '电话键盘 2→ABC…9→WXYZ，给一串号码枚举所有可能的字母组合，或找出其中有意义的单词。', cp: '朴素：递归枚举全部 ∏kᵢ 种组合，指数级 → 优化：结合字典前缀剪枝，前缀不在词典就停；或用"混合进制计数"迭代逐位取模生成，避免递归栈开销。' };
  X.m41 = { q: '定义"修改/插入/删除"各计一次操作，如何量化两个字符串的相似度？', cp: '朴素：枚举操作序列，指数级 → 优化：编辑距离 DP——d[i][j] 由左/上/左上三格转移，O(nm) 时间 O(min(n,m)) 滚动空间；相似度 = 1/(距离+1)。' };
  X.m42 = { q: '只给待删节点的指针、没有头结点也拿不到前驱，如何在单链表里 O(1) 删掉它？', cp: '朴素：从头扫找前驱 O(n) → 优化：后继覆盖法——把 next 的数据覆盖进待删节点、删除 next，O(1)；尾节点无后继仍需 O(n) 或懒删除。' };
  X.m43 = { q: '搜索引擎要从长文档里截一段同时包含全部关键词的最短文字做摘要，怎么高效找？', cp: '朴素：枚举所有区间 O(P²) → 优化：收集全部 (位置,词) 排序后双指针滑窗——凑齐就收缩，O(PlogP + P)。' };
  X.m44 = { q: '两条单链表若相交呈 Y 形，如何判断是否相交？又如何找出交点？', cp: '朴素：双重遍历两两比地址 O(nm) → 优化：① 比尾节点地址 O(n+m)；② 双指针各走完自己再换对方链，路长自动拉平，恰在交点相遇，O(n+m) 且 O(1)。' };
  X.m45 = { q: '设计一个队列，enqueue、dequeue、max 三个操作都要 O(1)（均摊）。', cp: '朴素：max 扫全队列 O(n) → 优化：① 带 max 的双栈拼队列——栈每层记历史 max，倒栈均摊 O(1)；② 滑动窗口最大值用单调递减双端队列，每元素进出各一次，O(n)。' };
  X.m46 = { q: '把二叉树看成图，求相距最远的两个节点间的路径长（不一定过根）。', cp: '朴素：每对节点求路径 O(n²) → 优化：后序递归一趟——每节点上传"向下深度"，用 左深+右深 更新全局直径，O(n) 时间 O(树高) 栈。' };
  X.m47 = { q: '由前序与中序遍历结果重建唯一的二叉树。为什么前序+后序不行？', cp: '朴素：每层在中序里线性找根 O(n²) → 优化：哈希存中序下标后分治递归，每节点 O(1) 定位，总 O(n)；前序+后序无法区分单子树方向故不唯一。' };
  X.m48 = { q: '按层从上到下、层内从左到右输出二叉树（分层遍历）。', cp: '朴素：递归 dfs(node,k) 逐层打印，每层全树扫一遍 O(n×高) → 优化：单队列 BFS + 记每层开头的队长作边界，每节点进出队各一次 O(n)；双队列一层一倒等价。' };
  X.m49 = { q: '给一段"看起来对"的代码找 bug：无符号倒计数、二分中点、数组边界、指针操作……高频雷区在哪？', cp: '方法：找不变量 → 卡边界（n=0/1/满） → 查类型溢出（unsigned 回绕、(lo+hi)/2 溢出） → 手动模拟小例子；工程上配断言、静态分析与边界单测。' };

  /* ---- 第四辑 数学与概率（m50..m59） ---- */
  X.m50 = { q: '100 个座位的飞机，第 1 位乘客（金刚）随机乱坐，之后每人对号入座、发现被占就随机挑空位。最后一位乘客坐到自己座位的概率是多少？', cp: '朴素：模拟所有落座序列，阶乘级 → 优化：递推 P(n)=1/n+Σ(1/n)P(n−k+1)，归纳得 P≡1/2；或用"1 号座与 n 号座对称"一步看穿，O(1)。' };
  X.m51 = { q: '用 1×2 瓷砖盖满 2×n 地板有多少种盖法？换 L 形三连块呢？', cp: '朴素：回溯枚举所有铺法，指数级 → 优化：看最左端分类——竖放剩 f(n−1)、横放成对剩 f(n−2)，f(n)=Fib(n+1) 递推 O(n)；L 形块联立辅助序列仍是线性递推，矩阵快速幂 O(logn)。' };
  X.m52 = { q: '2n 人买 5 元票，一半持 5 元一半持 10 元，售票处开始时没钱。能随时找零的排队方案有多少种？', cp: '朴素：枚举全排列逐个验证前缀和，O((2n)!) → 优化：反射原理——合法数 = C(2n,n)−C(2n,n−1) = Catalan 数 C(2n,n)/(n+1)，再乘两组内部排列 n!×n!，O(n) 预计算。' };
  X.m53 = { q: '判断点 P 是否在三角形 ABC 内部，只用乘法和比较、不用三角函数。', cp: '朴素：面积法要开方且需浮点容差 → 优化：同侧法——三个叉积 cross(AB,AP) 等符号一致即在内部，只用乘法与符号比较 O(1)；重心坐标法同理。' };
  X.m54 = { q: '磁带上顺序存 n 个文件，读第 i 个要扫过前面所有长度。按什么顺序存放使平均读取时间最小？', cp: '朴素：枚举所有排列 O(n!) → 优化：比值贪心——按 频率/长度 降序排列，交换相邻逆序对必使代价下降（交换论证），排序 O(nlogn) 即最优。' };
  X.m55 = { q: '1 米杆上 5 只蚂蚁各自朝一端走，碰头就回头。全部落杆的最短/最长时间是多少？', cp: '朴素：模拟碰撞序列，指数级分支 → 优化：碰头回头 ≡ 碰头互穿（蚂蚁不可区分）——每只当独自走到底：最短 max(min(x,L−x))、最长 max(max(x,L−x))，O(n) 一步算完。' };
  X.m56 = { q: '给"判断三角形类型（等边/等腰/不等边/非三角形）"的程序设计完整测试用例。', cp: '方法：等价类划分——每个输出类至少一条；等腰要试三种位置；边界值——两边和恰等于第三边（1,2,3）；再加非法输入（0/负/非数值）与溢出用例，十几条才完整。' };
  X.m57 = { q: '给定部分填好的数独残盘，它有多少个不同解？如何验证一道题是否唯一解？', cp: '朴素：盲回溯枚举，6.67×10²¹ 终盘空间 → 优化：候选集约束传播（行/列/宫剔除、单元传播）+ 候选最少优先（MRV）回溯计数，验唯一性时 count≥2 即剪枝退出。' };
  X.m58 = { q: '字母代数字的竖式谜题（SEND+MORE=MONEY，字母互不相同），求每个字母的值。', cp: '朴素：枚举 P(10,8)≈180 万种赋值 → 优化：进位结构分析——和多一位→M=1、百位进位→O=0，再逐列 DFS 带进位剪枝，搜索树降到几千；通用框架适用于一切字母算术谜题。' };
  X.m59 = { q: '扫雷中点开"1"后周围恰两个未开格，每格是雷的概率是 1/2 吗？两数字共享未开格时呢？', cp: '朴素：对称直觉直接分 1/2 → 共享局面会错 → 优化：条件概率——枚举满足所有相邻数字约束的雷布局，P(x 是雷)=含 x 的布局数/总布局数；区域小可精确枚举，O(2^区域)。' };
})();
