/* 工程实践 14 题文案增强：问题描述（q）+ 优化前后复杂度对比（cp）
   键名 pN（N=1..14），与 data_p.js 的 no 一致，加 p 前缀避免与经典谜题撞键 */
(function () {
  PZ.desc = PZ.desc || {};
  var X = PZ.desc;

  /* ---- 第一辑 数据与表示（p1..p3） ---- */
  X.p1 = { q: '给定 [0,10^7) 范围内最多 1000 万个无重复的非负整数，如何在约 1MB 内存内把它们排序并顺序输出？', cp: '朴素：比较排序（快排/归并）O(n log n) 次比较，且 1000 万个 int 远超内存 → 外部排序多趟磁盘归并 → 优化：位图——每个整数 1 比特，"读入置位 + 扫位输出"两遍线性扫描，时间 O(n)、空间 10^7 bit ≈ 1.25MB，零比较、零外排。' };
  X.p2 = { q: '字典中有 n 个英文单词，如何高效找出所有互为变位词的组（字母相同、顺序不同，如 care/race/acre）？', cp: '朴素：两两比较字母计数 O(n²·L)，10^5 词即 10^10 对 → 优化：规范签名——单词字母排序作签名，变位词 ⟺ 签名相同，按签名哈希/排序分组，O(n·L log L) 一遍完成。' };
  X.p3 = { q: '写一个把"月/日"换成"一年中第几天"的函数（如 3 月 1 日 = 平年第 60 天），如何避免写 12 层分支、还让闰年规则不改代码？', cp: '朴素：switch/if 逐月累加 12 个分支，闰年再翻倍分支 → 优化：表驱动——预计算累计天数表 cum[m]，doy = cum[m] + d 一次查表 O(1)；闰年只换一张 cum_leap 表，代码零改动。' };

  /* ---- 第二辑 性能与设计（p4..p6） ---- */
  X.p4 = { q: '不用计算器，如何心算回答"一个量每期增长 8%，多少期翻一倍"，并判断这个估算是否可信？', cp: '朴素：解 1.08^t = 2 得 t = ln2/ln1.08 ≈ 9.006，需查对数表 → 优化：72 法则——t ≈ 72 ÷ 8 = 9 期，一步心算，与精确解误差 < 0.1%；量级决策足够。' };
  X.p5 = { q: '在数组 a 中线性查找目标 x：如何把循环每轮的条件比较从 2 次降到 1 次，且不改变语义？', cp: '朴素：while (i < n && a[i] != x) 每轮 2 次比较、边界检查共 n+1 次 → 优化：哨兵——先 a[n] = x，循环缩为 while (a[i] != x)，停点 i == n 即不存在，循环后 1 次判断换掉循环内全部边界检查。' };
  X.p6 = { q: '有序稀疏整数集 {3,7,8,9,240,241,242} 如何用最少的字节存下来？', cp: '朴素：int32 数组 7×4 = 28 B；位图按值域开位 243 bit ≈ 31 B 反而更费 → 优化：差值 + 变长整数——存相邻差（多为 1 字节小数），每 7 位一组加延续位，共 8 B，压缩约 3.5 倍。' };

  /* ---- 第三辑 排序与取样（p7..p8） ---- */
  X.p7 = { q: '同一个排序任务，插入排序与快速排序各自适合什么数据？为什么标准库排序常是两者混合？', cp: '插入：最佳 O(n)（近序）最坏 O(n²)；快排：期望 O(n log n) 原地，但坏 pivot 退化为 O(n²) → 工程：小块/近序用插入、大块用随机 pivot 快排，混合体各取所长。' };
  X.p8 = { q: '从 1..n 中均匀、无放回地抽 m 个数，如何保证抽取次数有界（恰好 m 次）？', cp: '朴素：抽到重复就丢弃重抽——重试次数无界、分布易写错 → 优化：Floyd——j = n−m+1..n 抽 t = randint(1, j)，t 不在 S 中则放 t、否则放 j；恰好 m 轮、集合大小恒为 m、均匀性可证。' };

  /* ---- 第四辑 结构与文本（p9..p11） ---- */
  X.p9 = { q: '动态数字集合上反复"取当前最大 / top-k"：如何让插入与取极值都是 O(log n)？', cp: '朴素：排序再取 O(n log n) 一次性、或逐次线性扫 O(n)/次 → 优化：二叉堆——完全二叉树存数组，插入 sift-up / 取根 sift-down 均 O(log n)；建堆 O(n)，堆排序原地 O(n log n)，top-k 只维护大小 k 的堆。' };
  X.p10 = { q: '统计文章中每个词的出现次数并输出前 k 高频：如何避免把全部单词排序？', cp: '朴素：全排序再跑一遍计数 O(N log N) → 优化：哈希计数 count[w]++ 一遍 O(N)；top-k 维护大小 k 的小根堆 O(D log k)（D 为不同词数），全表永远不排序。' };
  X.p11 = { q: '如何随机生成一段指定长度、"读起来像原文"又不是抄原文的文本？', cp: '朴素：均匀随机选词 = 词沙拉；照抄原文 = 抄袭 → 优化：k 阶 Markov 链——统计"前 k 词状态 → 后续词表"，在表上随机游走生成；k=2~3 即有像而不抄的效果，时间 O(生成长度)。' };

  /* ---- 第五辑 方法与正确性（p12..p14） ---- */
  X.p12 = { q: '如何论证一段循环（如求前缀最大值）在"试了几个用例都过"之外确实是对的？', cp: '朴素：靠手工测试与代码评审，off-by-one 易漏网 → 方法：循环不变量——写下每轮循环前都成立的命题，按初始化/保持/终止三步证明；终止时不变量直接交出后置条件，正确性 = 数学归纳法。' };
  X.p13 = { q: '快排等实现写完后，除了手挑几个用例，如何获得充分的测试覆盖？', cp: '朴素：手工造少量用例（有序/逆序/含重复），盖不住边界与坏运气 → 方法：脚手架——随机生成器 + 暴力 oracle + 比较器做差分测试，百万轮对账并留存最小失败用例；再在不变量处种 assert 定位到行。' };
  X.p14 = { q: '程序慢了该按什么顺序优化？为什么"直接钻进代码调优"常常吃亏？', cp: '朴素：一头扎进调优/换语言/加机器，收益几个百分点到几倍 → 方法：攻击层级自顶向下——问题定义、算法、数据结构、代码调优、硬件，越上层收益越大（量级 vs 倍数），上层红利吃尽才下探。' };
})();
