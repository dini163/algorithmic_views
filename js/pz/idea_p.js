/* 工程实践 14 题解题思路：分步骤讲清"怎么想到、怎么解出"（渲染进大白话区块）
   键名 pN，与 desc_p/extra_p 一致 */
(function () {
  PZ.idea = PZ.idea || {};
  var X = PZ.idea;

  /* ---- 第一辑 数据与表示（p1..p3） ---- */
  X.p1 = '① 内存只有 1MB 却要排 1000 万个 int：每个 int 存 4B 都装不下，比较排序先排除；② 抓住题眼"无重复 + 值域有界"——每个整数只需 1 个比特表示"在不在"，10^7 bit ≈ 1.25MB 刚好；③ 读入遍：读到一个数 x 就 bit[x]=1，一遍 O(n)；④ 输出遍：i 从 0 扫到 10^7−1，bit[i]=1 就输出 i，天然升序；⑤ 两遍扫描、零比较、无外排——用 n/8 字节的空间换掉所有比较与磁盘趟数。';
  X.p2 = '① 两两判断变位词是 O(n²) 对比较，词典一大就不可行；② 找不变量：变位词的字母多重集完全相同；③ 把字母排序得到规范形（签名），care/race/acre 都变成 acer；④ 于是"变位词"等价于"签名相同"，问题降维成按键分组；⑤ 哈希或排序签名一遍扫完，同桶单词一起输出，总代价 O(n·L log L)。';
  X.p3 = '① 12 个分支的 switch 本质是把"前几个月共多少天"这份知识硬编码进代码；② 把知识搬出去：months[12] 存每月天数；③ 再预计算累计表 cum[m] = 前 m−1 个月总天数，启动时算一次；④ 转换只剩一次查表：doy = cum[m] + d，O(1) 且无分支；⑤ 规则变化（闰年/财年历）只换表不改码——表驱动的精髓是让变化落在数据上。';

  /* ---- 第二辑 性能与设计（p4..p6） ---- */
  X.p4 = '① 精确解 t = ln2/ln(1+r) 要查对数，心算不可能；② 小 r 时 ln(1+r) ≈ r，于是 t ≈ 0.693/r；③ 把分子换成约数多的 72（百分数形式）：t ≈ 72 ÷ r%；④ 8% → 72/8 = 9 期，校验 1.08^9 = 1.999，误差不到 0.1%；⑤ 估算的目的不是精确，而是在设计与评审里快速淘汰量级错误的方案。';
  X.p5 = '① 朴素线性查找循环里有两个条件：边界 i<n 与取值 a[i]!=x，其中边界检查在前 n−1 轮都是浪费；② 想法：让"越界"不可能发生——把 x 种到 a[n] 当哨兵；③ 循环缩成 while (a[i] != x) i++，必然停住，最坏停在哨兵；④ 事后判读：i == n 说明撞哨兵 = 不存在，否则真命中；⑤ 哨兵是通用手法：字符串 \\0、链表尾节点、匹配器边界都是它，配合循环不变量外提等纪律构成代码调优基本功。';
  X.p6 = '① 先算朴素账：int32 × 7 = 28 B；② 候选一位图：空间取决于值域（243 bit ≈ 31 B），稀疏宽值域反而更费——位图只赢在稠密小值域；③ 利用"有序"：存相邻差 delta，成簇使差多为 1 这样的小数；④ 再用 varint（每 7 位一组、最高位延续）打包小数，231 占 2 字节，共 8 B；⑤ 结论：表示服务于数据形状——先看分布（稠密/稀疏、有序/无序、值域宽窄）再选表示。';

  /* ---- 第三辑 排序与取样（p7..p8） ---- */
  X.p7 = '① 插入排序把前缀当理好的牌，新牌左移插入，最佳 O(n) 最坏 O(n²)；② 快速排序选 pivot 一趟分区：小于 pivot 的去左、大于的去右，pivot 落到最终位置；③ 两边递归，期望深度 O(log n)，总代价 O(n log n)；④ 但两者各有死穴：插入怕逆序、快排怕对抗 pivot；⑤ 工程答案是混合：小块切插入、随机 pivot 防最坏——标准库排序几乎全是这样的混合体。';
  X.p8 = '① "抽到重复就重抽"看似直观，但重试次数无界、分布还容易写错；② Floyd 反向思考：不重试，让 j 从 n−m+1 走到 n；③ 每轮抽 t = randint(1, j)：t 不在 S 就放 t，否则放 j 本身；④ 这条撞车规则是均匀性的关键：不变量"处理完 j 后 S 是 1..j 的均匀 (j−n+m)-样本"可归纳证明；⑤ 循环恰好 m 轮、|S| 恒为 m；流式未知 n 的场景换同家族的蓄水池抽样。';

  /* ---- 第四辑 结构与文本（p9..p11） ---- */
  X.p9 = '① 需求是"动态取最大"：排序再取是一次性的，逐次线性扫每次 O(n)；② 二叉堆 = 完全二叉树存数组，节点 i 的孩子在 2i+1/2i+2，零指针开销；③ 只维护一条弱序：父 ≥ 子（大根堆），于是根永远是最大值；④ 插入沿路上浮、取根把根换到末尾再下沉，都是 O(log n)；建堆从最后非叶往上 sift-down，合计 O(n)；⑤ 堆排序 = 反复取根原地完成；top-k 维护大小 k 的堆，空间 O(k)。';
  X.p10 = '① 把全部单词排序是 O(N log N)，但目标只要计数、不要全序；② 哈希计数：逐词 count[w]++，一遍 O(N)，内存 = 不同词数 D；③ top-k 不全表排序：维护大小 k 的小根堆，新计数大于堆顶才入、入则弹顶，O(D log k)；④ 分布式把同一份计数分片去做（MapReduce wordcount）：各片本地计数、merge 相加；⑤ 流式有界空间用 Misra-Gries / Count-Min Sketch 以精度换空间。';
  X.p11 = '① 纯随机是词沙拉、照抄是抄袭，要的是"像而非抄"；② k 阶 Markov：把前 k 个词当状态，扫原文一遍记录每个状态的后续词表；③ 生成 = 在表上随机游走：从初始状态随机选一个后续、窗口滑动继续；④ k 是旋钮：k=0 退化成词沙拉、k 太大趋近照抄，k=2~3 最像；⑤ 同族谱系：n-gram 语言模型、输入法/代码补全的下一词预测，都是"谁后面常接谁"的统计。';

  /* ---- 第五辑 方法与正确性（p12..p14） ---- */
  X.p12 = '① 测试只能证有 bug、不能证无 bug，循环需要一个"契约"——循环不变量；② 不变量是每轮循环前状态都成立的命题：前缀最大值就是 m = max(a[0..i])；③ 三步证明：初始化在循环前成立、保持每轮不破、终止时与退出条件合出后置条件；④ 这正是数学归纳法：初始化 = 基础步，保持 = 归纳步；⑤ off-by-one 往往不破"保持"而破"终止"：不变量仍真但交不出后置条件，第三步当场暴露；把不变量 assert 进代码，证明就机械化了。';
  X.p13 = '① 手写用例有三缺：数量少、盖不住边界、失败难复现；② 脚手架三件：生成器随机造输入、暴力 oracle 给标准答案、比较器逐一对账——差分测试；③ oracle 允许慢但必须显然对（插入排序/暴力枚举），被测实现才是快版本；④ 不一致时脚手架当场留存最小失败用例，复现与回归一步到位；⑤ 再在前置/不变量/后置处种 assert：脚手架回答"哪例错"，断言回答"哪行错"，双保险。';
  X.p14 = '① 性能的攻击层级有五层：问题定义、算法设计、数据结构、代码调优、硬件；② 越上层收益越大：改需求（只要 top-10）、换算法（O(n²)→O(n)）是量级，调优与硬件只有倍数；③ 所以纪律是自顶向下：先问问题能不能重定义，再看算法与结构，最后才抠代码；④ 每层之间要度量：达标即停，不为下探而下探；⑤ 本库的 p4 估算决定"要不要优化"，p5 调优与 p6 表示是下层工具，本题是它们的总纲。';
})();
